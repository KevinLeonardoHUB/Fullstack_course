3 components:
- Header - Renders the name
- Content -  Renders the parts and exercise numbers
- Total - Renders the total number of exercises

All data inside the component *App*
Use props to pass necessary data
to other components
const part1 = 'Fundamentals of React'
  const exercises1 = 10
  const part2 = 'Using props to pass data'
  const exercises2 = 7
  const part3 = 'State of a component'
  const exercises3 = 14'
        <p>Number of exercises {exercises1 + exercises2 + exercises3}</p>

        const Total = (props) => {
  return (
    <div>

    </div>
  )
}