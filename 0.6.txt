sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note and clicks Save

    Note right of browser: JavaScript prevents the default form submission

    Note right of browser: Browser creates the new note, adds it to the list and redraws the notes

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: The new note is sent as JSON
    activate server

    server-->>browser: HTTP 201 Created
    deactivate server

    Note right of browser: Browser stays on the same page